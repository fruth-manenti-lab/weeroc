# 9 September 2026 — transport and ownership validation

## Setup

- macOS ARM64; Python 3.13; rebuilt package 0.2.0.
- User reports the board is powered, with no SiPM or pulse generator connected.
- FTDI candidates: `/dev/cu.usbserial-RD3_320` and
  `/dev/cu.usbserial-RD3_321`, description `PCB_RADIOROC`, VID:PID `0403:6010`,
  serial `RD3_32`, location `1-1`.
- Both ports resolve to board identity `usb:0403:6010:serial:RD3_32`.

## Read-only checks

Command: `.venv-foundation/bin/python scripts/radioroc_check_connection.py --timeout 1`

Result: `OK: address 100 = 00000101 (5)` on the established control interface
`/dev/cu.usbserial-RD3_320`.

A separate check opened that interface, read status 5 again, and held ownership
while a subprocess attempted the same connection check through
`/dev/cu.usbserial-RD3_321`. The subprocess exited with code 1 and
`DeviceBusyError`, identifying the existing owner on the first interface.
It was blocked before serial open. The owner closed afterward and released
the lease. These checks issued no FPGA/ASIC configuration writes.

Two earlier planning-time reads had timed out. Communication now succeeds;
the cause of the earlier failures has not been established.

## Offline checks and limits

Implementation checkpoint: `6c44092`. All 30 unit tests and 15 CLI help checks
passed, including subprocess lock contention and release after process kill.
The built wheel passed checks outside the checkout and rendered a synthetic
threshold plot. No scan, sensor response, calibration or Linux USB validation
was performed. See `IMPLEMENTATION_STATUS.md` for remaining work.
